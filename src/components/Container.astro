---
import { cn } from "../lib/utils";

type Props = {
  className?: string;
};

const { className = "" } = Astro.props;
---

<div class={cn("max-w-7xl w-full px-4 lg:px-0 mx-auto", className)}>
  <slot />
</div>
